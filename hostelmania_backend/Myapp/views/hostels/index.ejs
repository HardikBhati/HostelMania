<%- include("../partials/header") %>

<div class="container">
    <header class="jumbotron">
        <div class="container">
            <h1> Welcome to HostelBlogs </h1>
            <p>Take a look at the best hostels in the world!</p>
            <p>
                <a class="btn btn-primary btn-lg" href="/hostels/new">Add new hostels</a>
            </p>
        </div>
    </header>


    <div class="row text-center container" style="display: flex; flex-wrap: wrap;">
        <% for (let i=0;i<=hostel.length-1;i++) { %>
            <div class="col-md-4 col-sm-6">

                <div class="thumbnail">
                    <img src=<%= hostel[i]['image'] %> alt="image of hostel">
                    <div class="caption">
                        <%= hostel[i]["name"] %>
                    </div>
                    <p>
                        <a class="btn btn-primary" href="/hostels/<%= hostel[i]['id'] %>">More Info</a>
                    </p>
                </div>
                
            </div>
        <% } %>        
    </div>

</div>

<%- include("../partials/footer") %>
